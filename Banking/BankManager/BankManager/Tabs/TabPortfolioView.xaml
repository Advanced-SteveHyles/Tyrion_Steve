<UserControl x:Class="PortfolioManager.TabPortfolioView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"    
             xmlns:local="clr-namespace:PortfolioManager"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions></Grid.ColumnDefinitions>

        <local:SearchPortfolioView DataContext="{Binding SearchPortfolioVM}"  Grid.Row="2"></local:SearchPortfolioView>
        <local:DataEntryPortfolioView DataContext="{Binding DataEntryPortfolioVM}" Grid.Row="3"></local:DataEntryPortfolioView>
    </Grid>
</UserControl>
