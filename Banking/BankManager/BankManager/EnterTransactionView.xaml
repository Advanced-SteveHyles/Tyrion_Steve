<UserControl x:Class="PortfolioManager.EnterTransactionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
    <StackPanel>
        <TextBlock>Date</TextBlock>        
        <RadioButton>Reconciled Date</RadioButton>
            <DatePicker SelectedDate="{Binding ReconciledDate}"></DatePicker>
            <RadioButton>Estimated Date</RadioButton>
        <DatePicker SelectedDate="{Binding EstimatedDate}"></DatePicker>
            
            <TextBlock>TransactionType</TextBlock>
            <ComboBox ItemsSource="{Binding TransactionTypes}" Text="{Binding TypeName}" />


            <TextBlock>Amount</TextBlock>
        <TextBox></TextBox>
        
        <StackPanel Orientation="Horizontal">
                <Button>New</Button>
            <Button>Save</Button>
            <Button>Cancel</Button>
        </StackPanel>
    </StackPanel>
    </Grid>
</UserControl>
